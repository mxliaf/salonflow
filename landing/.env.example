# URL da API do backend (para possíveis integrações futuras do lado do servidor)
PUBLIC_API_URL=http://localhost:8000

# URL do aplicativo principal (dashboard de agendamento)
PUBLIC_APP_URL=http://localhost:3000
